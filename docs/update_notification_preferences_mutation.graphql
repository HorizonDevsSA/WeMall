mutation UpdateNotificationPreferences($category: NotificationCategory!, $emailEnabled: Boolean!, $pushEnabled: Boolean!) {
  updateNotificationPreferences(category: $category, emailEnabled: $emailEnabled, pushEnabled: $pushEnabled) {
    category
    emailEnabled
    pushEnabled
  }
}
query GetCart {
  cart {
    id
    userId
    itemCount
    subtotal
    items {
      id
      variantId
      productId
      quantity
      unitPrice
      productTitle
      variation
      variationThumbnail
      sellerId
      storeTitle
      storeLogo
    }
  }
}
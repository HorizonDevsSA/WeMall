mutation CreateFlashSale($input: CreateFlashSaleInput!) {
  createFlashSale(input: $input) {
    id
    name
    startTime
    endTime
    status
    items {
      id
      productId
      discountPrice
      stockLimit
    }
  }
}

mutation OpenDispute($orderId: ID!, $reason: String!, $evidenceUrls: [String!]) {
  openDispute(orderId: $orderId, reason: $reason, evidenceUrls: $evidenceUrls) {
    id
    orderId
    buyerId
    sellerId
    reason
    status
    createdAt
  }
}
mutation CreateStore($input: CreateStoreInput!) {
  createStore(input: $input) {
    id
    userId
    storeName
    storeSlug
    logoUrl
    bannerUrl
    description
    status
    isVerified
  }
}